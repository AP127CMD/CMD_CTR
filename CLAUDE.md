# CMD CTR — Claude Code Context

## What this project is
Real-time flight-schedule dashboard highlighting AP-127 across 8 views (Day Glance, Board, Gantt, Weekly, Analytics, Roster, Slot Finder, Auto Slot Finder).
GitHub: `AP127CMD/CMD_CTR`. Live: `https://ap127-cmd-ctr.pages.dev`. Local: `/Users/nugui/flight-schedule-feed/`.

## Current state
- Version token: `?v=r40` on all `<script>` tags in `index.html`
- Last significant change: 2026-05-30 (r40 — DAY GLANCE hours, GANTT FI-as-SP, ANALYTICS completed hrs, AUTO SLOT FINDER clickable timeline)
- ⚠️ **Cache issue:** r41 (Calendar richer cells) and r42 (BOARD hours, GANTT dynamic scroll) JS changes were committed AFTER r40 but the `?v=r40` token was NOT bumped for them — browsers with cached JS may serve stale r40 JS for r41/r42 features. Next change MUST bump to `?v=r43`.
- Active branch: main (direct-to-main workflow)

## Key facts
- React 18 CDN UMD + Babel Standalone — no build step; transpiled in-browser
- `flight-data.js` is AUTO-GENERATED by GitHub Actions cron (`fetch_schedule.yml`, every 30 min via `ap127-dispatcher`) — **do not edit manually**
- Bump the `r` version token on every change to any file in `js/` — next release must be `r43` (r41/r42 were unbumped)
- Hosting: Cloudflare Pages Git-integrated — push to main auto-deploys to `ap127-cmd-ctr`
- Local preview: `python3 -m http.server 7420 --directory /Users/nugui/flight-schedule-feed`
- Old repo `nuguitar/AP127_Command_Center` is **private/archived** — use `AP127CMD/CMD_CTR` only
- Full field schema (27 fields per schedule entry) and view-by-view behaviour is documented in AP127_Docs

## Update rule
After every code change in this session:
1. Bump `?v=rNN` token in `index.html`
2. Update this file (version token, current state section above)
3. Update `/Users/nugui/AP127_Docs/README.md` (§2.1)
4. `git add CLAUDE.md && git commit && git push` this repo
5. `cd /Users/nugui/AP127_Docs && git add README.md && git commit -m "docs: ..." && git push`

## Master reference
Full architecture, deployment steps, secrets, and reproduce-from-scratch guide:
https://ap127-docs.pages.dev  (source: `/Users/nugui/AP127_Docs/README.md`)
